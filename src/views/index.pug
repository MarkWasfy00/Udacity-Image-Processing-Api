doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title ImgProcessingApi
        script(src="https://cdn.tailwindcss.com") 
    body
    main(class="container px-4 mx-auto flex flex-col gap-5")
        h2(class="text-center font-semibold text-2xl pt-5") Welcome to Udacity image processing api by <a href="https://www.linkedin.com/in/markwasfy00/" class="text-sky-500 underline" target="_blank">Mark Wasfy 🤹‍♂️</a> 
        form(action="/api/images" class="flex flex-col gap-3 shadow-md p-6" , method="get")
            select(name="filename" class="border-2  p-2 border border-sky-500 rounded focus:outline-none") 
                each val in (fullFolder || [])
                    option(value=val)= val
            input(type="number", name="width" class="border-2 p-2 border border-sky-500 rounded focus:outline-none" required placeholder="width")
            input(type="number", name="height" class="border-2 p-2 border border-sky-500 rounded focus:outline-none" required placeholder="height")
            input(type="submit", value="resize" class="transition p-2 text-white cursor-pointer ease-in-out delay-150 rounded bg-sky-500 hover:bg-sky-600 focus:outline-none")
        div(class="flex flex-col p-2 gap-5 shadow-md")
            div(class="font-semibold text-2xl") History ⏱
            div(class="flex flex-wrap gap-5")
                each img in (thumbFolder || [])
                    a(href=`${process.env.IMAGE_OUTPUT_STATIC_FOLDER}${img}` class="overflow-hidden" style={'width': '100px', 'height': '100px'})
                        img(src=`${process.env.IMAGE_OUTPUT_STATIC_FOLDER}${img}`, class="object-contain h-48 w-96" alt=img) 

        form(action="/api/images", class="flex flex-col gap-3 shadow-md p-6" enctype="multipart/form-data" method="post") 
            input(type="file", name="image")
            input(type="submit", value="upload" class="transition p-2 text-white cursor-pointer ease-in-out delay-150 rounded bg-sky-500 hover:bg-sky-600 focus:outline-none")
    script(src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.bundle.min.js") 